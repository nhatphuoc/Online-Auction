# k8s/ingress.yaml - placeholder for Kubernetes ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: online-auction-ingress
  namespace: online-auction
spec:
  rules:
    - host: example.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: auction-service
                port:
                  number: 80
