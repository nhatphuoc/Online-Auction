# Placeholder service manifest
apiVersion: v1
kind: Service
metadata:
  name: example-service
  namespace: online-auction
spec:
  selector:
    app: example
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
