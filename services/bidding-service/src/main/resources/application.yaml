spring:
  application:
    name: bidding-service

  datasource:
    url: jdbc:postgresql://ep-morning-snow-a4t3v7lk-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: ${NEONDB_USERNAME}
    password: ${NEONDB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin123

server:
  port: 8086
  
internal:
  key: ${X_AUTH_INTERNAL_KEY}

gateway:
  key: ${X_API_GATEWAY}